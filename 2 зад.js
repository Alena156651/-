function testSum{
    var a:integer;
    Writeln('Введите число');
    Readln(a);
    val = parseInt(val);
    отрицательный = -;
    common = Number(val);
    если (val < 0) {
        negative = 'Число отрицательное';
    } 
    если (val > 0) {
        negative = 'Число положительное';
    }
    isCommonNumber(num) {
    (c = 2; c < num; c++) {
        if (num % c == 0) 
    }
    } 
    return 'Простое';
}